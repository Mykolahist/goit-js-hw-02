function getExtremeElements(array) {
    // Пиши код ниже этой строки
    array = [array[0], array[array.length - 1]];

    return array;
    // Пиши код выше этой строки
}
  
console.log(getExtremeElements('firestarter'));